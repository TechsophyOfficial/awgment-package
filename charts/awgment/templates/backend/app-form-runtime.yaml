---
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .Values.runtimeformapp.image.configMapRef }}
  namespace: {{ .Values.namespace }}
data:
  GATEWAY_URI: {{ .Values.gateway.url }}
  KEYCLOAK_REALM_NAME: {{ .Values.keycloak.realm }}
  KEYCLOAK_URL_AUTH: {{ .Values.keycloak.url }}
  TP_MODELER_APP_MONGO_URI: {{ .Values.mongo.url }}
  tp_modeler_app_mongoUri: {{ .Values.mongo.url }}
  ELASTIC_ENABLE: "{{ .Values.runtimeformapp.elastic }}"
  ELASTIC_SOURCE: "{{ .Values.runtimeformapp.elastic }}"

---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Values.runtimeformapp.name }}
  namespace: {{ .Values.namespace }}
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      name: {{ .Values.runtimeformapp.name }}
  template:
    metadata:
      labels:
        name: {{ .Values.runtimeformapp.name }}
    spec:
      containers:
      - image: {{ .Values.runtimeformapp.image.repository }}:{{ .Values.runtimeformapp.image.tag }}
        name: {{ .Values.runtimeformapp.name }}
        envFrom:
        - configMapRef:
            name: {{ .Values.runtimeformapp.image.configMapRef }}
        ports:
        - containerPort: {{ .Values.runtimeformapp.service.port }}
        imagePullPolicy: {{ .Values.imagePullPolicy }}
      imagePullSecrets:
      - name: {{ .Values.imagePullSecrets }}



---
apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.runtimeformapp.name }}
  namespace: {{ .Values.namespace }}
spec:
  type: {{ .Values.runtimeformapp.service.type }}
  ports:
  - port: {{ .Values.runtimeformapp.service.port }}
  selector:
    name: {{ .Values.runtimeformapp.name }}
